[Unit]
Description=arm_docker_env
After=display-manager.service
Requires=docker.service

[Service]
ExecStart=/usr/bin/docker run --rm --privileged multiarch/qemu-user-static --reset -p yes
Restart=on-failure
RestartSec=3s

[Install]
WantedBy=multi-user.target
